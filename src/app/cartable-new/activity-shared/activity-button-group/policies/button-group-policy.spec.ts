import { ButtonGroupPolicy } from './button-group-policy';

describe('ButtonGroupPolicy', () => {
  it('should create an instance', () => {
    expect(new ButtonGroupPolicy()).toBeTruthy();
  });
});
